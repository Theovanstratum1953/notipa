# Copy to .env for development, .env.prod for production, and fill in.
# On disk, rename this file to .env.example (or .env / .env.prod directly).

DJANGO_ENV=development
SECRET_KEY=change-me
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
CSRF_TRUSTED_ORIGINS=

# Only read when DJANGO_ENV=production
DB_ENGINE=django.db.backends.postgresql
DB_NAME=notipa
DB_USER=notipa
DB_PASSWORD=change-me
DB_HOST=db
DB_PORT=5432

# PostgreSQL image env vars (production only — must match DB_* above)
POSTGRES_DB=notipa
POSTGRES_USER=notipa
POSTGRES_PASSWORD=change-me

# Web Push (VAPID) — optional. Leave both keys blank and push notifications
# are simply unavailable (the "Enable notifications" control explains this);
# nothing else in the app depends on them. Generate a real pair once per
# environment (dev and production must NOT share one) with:
#     python -m py_vapid --gen
# then paste the raw base64 values below — never commit real values.
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_ADMIN_EMAIL=admin@notipa.org
